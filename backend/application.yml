spring:
  application:
    name: PackSimulator
  data:
    mongodb:
      database: packsimulator
      uri: mongodb://mongo:27017
  logging:
    level:
      root: info
      de.thro.packsimulator: debug
      org.springframework: warn
server:
  port: 8080
